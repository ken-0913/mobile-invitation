export const environment = {
  production: false,
  // 네이버 클라우드 플랫폼(Maps) 클라이언트 ID.
  // NCP 콘솔 > AI·NAVER API > Maps 에서 발급하고, 사용 도메인
  // (예: http://localhost:4200, https://invitation.ran-kun.com)을 등록해야 지도가 뜬다.
  // 신규 키는 ncpKeyId, 구 키는 ncpClientId 를 쓴다(아래 컴포넌트의 로더 참고).
  naverMapClientId: '4zjafa03ur',
};
